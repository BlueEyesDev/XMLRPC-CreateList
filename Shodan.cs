using System.Collections.Generic;
using System.Net;
using System.Web.Script.Serialization;
namespace XMLRPC_CreateList
{
    internal class Shodan
    {

        /* The code has been removed to avoid being used for malicious purposes */
        public Shodan(string API) {
            /* The code has been removed to avoid being used for malicious purposes */
        }
        public List<string> GetList(string Country = "") {
            /* The code has been removed to avoid being used for malicious purposes */ 
            return new List<string>() { };
        }
    }
}
